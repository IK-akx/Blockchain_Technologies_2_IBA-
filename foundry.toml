[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
cache_path = "cache"
solc_version = "0.8.24"
evm_version = "shanghai"
optimizer = true
optimizer_runs = 200

# Для coverage и отчетов
gas_reports = ["*"]